# 项目知识库

## Reactor 模式

Reactor 模式用于组织高并发网络事件处理。主线程监听连接和读写事件，事件到达后将任务分发给处理器或线程池执行，从而避免单线程阻塞。

## C++ 网关

本项目中的 C++ 网关负责 Linux/WSL 环境下的高并发接入、HTTP 请求读取、上游转发和 502 错误响应。Python 服务负责业务接口、模型调用、会话管理和持久化。

## 跨环境运行

项目支持 Windows、Linux、WSL 三类运行路径。Windows 侧适合演示前端和 Python 服务；WSL/Linux 侧适合验证 C++ epoll 网关。
